@page
@model HslHomepage.Pages.CasesModel
@{
    ViewData["Title"] = "Cases";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<div class="container" style="margin-top:20px">
    @await Component.InvokeAsync( "Division", new { description = "等待上架" } )
    <div class="row">
        <div class="col-md-4">
            @await Component.InvokeAsync( "CanvasLearn", new { description = "等待上架1" } )
        </div>
        <div class="col-md-4">
            @await Component.InvokeAsync( "CanvasLearn", new { description = "等待上架2" } )
        </div>
        <div class="col-md-4">
            @await Component.InvokeAsync( "CanvasLearn", new { description = "dhaihd" } )
        </div>
    </div>
</div>

